/**
 * Release 1 authorizes exactly 30 physical domain tables. This foundation unit
 * intentionally defines none; tables are activated only by later approved M2 units.
 */
export const release1DomainTableCount = 0 as const;
